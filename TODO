A FAIRE :
	fin de jeu [ améliorer le texte ]
	Rapport (exception, clavier, uml)

Idée d'amélioration
	Distance de vue sur la carte [ colorier les cases auquel le joueur peut acceder ] [ c.f code en bas ( attention les yeux codé en 5s )]

Si vous voyez n'importe quel bug. Ou si vous avez des idées envoyé moi un sms et mettez bien un jours se fichier


















DONT TOUCH PLZ


		if(caseActionnee != -1 && soldat[caseActionnee] instanceof Heros) {
			int x,y = 0;
			int distance = soldat[caseActionnee].getPortee();
			for(y = distance; y >= - distance; y--) {
				for(x = distance; x >= -distance; x--) {
					Position tmp = new Position(caseActionnee);

					int dxc = tmp.x + y;
					int dyc = tmp.y + x;

					int caseVoisine = dyc * IConfig.LARGEUR_CARTE + dxc;
					Position tmp2 = new Position(caseVoisine);
					if( tmp.distance(tmp2) <= distance) {
						if( (!tileset.getTile(carte[caseVoisine]).estPraticable() && tmp2.estValide() && soldat[caseVoisine] != null) || distance != 1)
						dessineRectangle(g, dxc, dyc, IConfig.SOLDAT_VISION_POSSIBLE);
					}


				}
			}
		}
		
